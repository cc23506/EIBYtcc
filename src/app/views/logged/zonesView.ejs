<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Visualizar Zonas</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .zone {
      position: absolute;
      border: 2px solid red;
      color: red;
      padding: 5px;
    }
    #image-container {
      position: relative;
    }
  </style>
</head>
<body>
  <h1>Zonas Disponíveis para <%= username %></h1> <!-- Nome do usuário -->

  <div id="image-container">
    <!-- Exibe a imagem mais recente -->
    <img src="<%= imagePath %>" alt="Imagem da Zona" id="zone-image" width="600">
    
    <!-- Exibe as zonas sobre a imagem, se existirem -->
    <% if (zones.length > 0) { %>
      <% zones.forEach(zone => { %>
        <div class="zone" style="left: <%= zone.x1 %>px; top: <%= zone.y1 %>px; width: <%= zone.x2 - zone.x1 %>px; height: <%= zone.y2 - zone.y1 %>px;">
          <%= zone.name %>
        </div>
      <% }) %>
    <% } else { %>
      <p>Nenhuma zona disponível para esta imagem.</p>
    <% } %>
  </div>

  <a href="/">Voltar</a>
</body>
</html>